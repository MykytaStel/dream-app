<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!-- Shown during cold start, before the React Native view mounts. The
         background matches the default theme so the hand-off has no white
         flash. On Android 12+ the system uses windowSplashScreenBackground and
         the launcher icon instead of windowBackground. -->
    <style name="LaunchTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:statusBarColor">@color/launch_background</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@drawable/ic_launcher_default_foreground</item>
    </style>

</resources>
