import { StyleSheet } from 'react-native';

export const statsScreenStyles = StyleSheet.create({
  summaryRow: {
    flexDirection: 'row',
    gap: 12,
  },
  sectionCard: {
    gap: 12,
  },
  sectionTitle: {
    fontWeight: '700',
  },
});
